// import { api } from '../../../api/index';
// import { loginReq } from '../../../scripts/loginReq';
// import { getProfileReq } from '../../../scripts/getProfileReq';

export const userRegistrationReq = async (values) => {
  // const response = await api.userRegistration.fetch(values);

  // if (response.status === 201) {
  //   const { data } = await response.json();
  //   console.log(data);
  // } else {
  //   const error = {
  //     status: response.status,
  //   };
  //   console.log(error);
  // }

  // if (values) {
  //   loginReq(values).then((status) => {
  //     if (status === 200) {
  //       getProfileReq();
  //     }
  //   });
  // }

  console.log('Send', values);
};